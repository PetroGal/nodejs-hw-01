import * as path from 'path';

export const PATH_DB_CONTACTS = '';

// const path1 = path.join(process.cwd(), 'src', 'db', 'db-contacts.json');
const path2 = path.resolve('src', 'db', 'db-contacts.json');
console.log(path2);
